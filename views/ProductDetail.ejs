<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <link rel="stylesheet" href="/css/stylesDetail.css">
  <title>Detalle del Producto <%= producto.id %></title>
</head>
  <%- include("partials/header") %>
<body>
  <main class="container">
    <div class="left-column">
      <img src="/images/<%= producto.imagen %>" width="700" height="700" alt="remera">
    </div>
    <div class="right-column">
      <div class="product-description">
        <span><%= producto.descripcion %></span>
        <h1><%= producto.nombre %></h1>
        <p>Talle <%= producto.talle %></p>
      </div>
      <div class="product-price">
        <span>$<%= producto.precio %></span>
      </div>
      <div class="button-options">
        <a href="/carrito" class="cart-btn">Añadir al carrito</a>
        <form action="/products/delete/<%= producto.id %>?_method=DELETE" method="POST" style="display: inline-flex">
          <button type="submit" class="action-button-delete">Eliminar</button>
        </form>
        <a href="/products/edit/<%= producto.id %>" class="action-button-edit">Editar producto</a>
      </div>
    </div>

  </main>



  <%- include("partials/footer") %>
</body>
</html>